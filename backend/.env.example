# PLANRAUM backend environment — all optional. The mock API runs without any.

# Comma-separated origins allowed by CORS (defaults to the Vite dev server).
# ALLOWED_ORIGINS=http://localhost:5173

# Anthropic API key — only needed when you wire real AI extraction (Claude) into
# app/services/extraction.py.
# ANTHROPIC_API_KEY=
